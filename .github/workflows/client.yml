# .github/workflows/client.yml
name: client
on:
  push:
    branches:
      - reference
jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout source code.
        uses: actions/checkout@v2
      - name: Install dependencies
        run: npm install
        working-directory: ./my-agora-states-client
      - name: Build
        run: npm run build
        working-directory: ./my-agora-states-client
      - name: SHOW AWS CLI VERSION
        env:
          AWS_ACCESS_KEY_ID: ${{ AKIASEBV4CULGZO2DR6A }}
          AWS_SECRET_ACCESS_KEY: ${{ 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 }}
          AWS_EC2_METADATA_DISABLED: true
        run: |
          aws --version
      - name: Sync Bucket
        env:
          AWS_ACCESS_KEY_ID: ${{ AKIASEBV4CULGZO2DR6A }}
          AWS_SECRET_ACCESS_KEY: ${{ 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 }}
          AWS_EC2_METADATA_DISABLED: true
        run: |
          aws s3 arn:aws:s3:::fe-47-hun0613 \
            --region ap-northeast-2 \
            build s3://<?> \
            --delete
        working-directory: <?>
